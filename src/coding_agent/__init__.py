"""Claude Code-like coding agent built on the deepagents library.

The compiled graph factory is exposed via ``coding_agent.agent:create_coding_agent``
and registered in ``langgraph.json`` (run with ``uv run langgraph dev``).
"""
